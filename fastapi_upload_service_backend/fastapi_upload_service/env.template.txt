# Copy this file to ".env" in this same folder (rename to .env).
# Lines starting with # are ignored if you use a simple parser; dotenv ignores # on full lines.

# Optional: smoother answers via DeepSeek (otherwise local OCR-based reply still works)
DEEPSEEK_API_KEY=

# CV is always run with project .venv\Scripts\python.exe if that exists (ignores CV_PYTHON).
# Advanced only: CV_FORCE_CV_PYTHON=1 then CV_PYTHON is honored.
# CV_FORCE_CV_PYTHON=
# CV_PYTHON=
