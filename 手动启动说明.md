# 手动启动（不用 .bat 脚本）

## 前端（Vite，端口 5173）

在 **PowerShell** 或 **CMD** 中：

```bat
cd /d D:\qianduan\upload-demo
npm install
npm run dev
```

浏览器打开：**http://127.0.0.1:5173/**

停止：在运行 `npm run dev` 的窗口按 **Ctrl + C**。

---

## 后端（FastAPI，端口 8000）

另开一个终端窗口：

```bat
cd /d D:\qianduan\fastapi_upload_service (5)\fastapi_upload_service
python main.py
```

或先激活虚拟环境再运行（若你平时这样用）：

```bat
.\.venv\Scripts\activate
python main.py
```

停止：**Ctrl + C**。

---

## 顺序建议

1. 先启动 **后端**（8000）  
2. 再启动 **前端**（5173）  
3. 前端会通过代理访问本机 8000，无需改代码。

---

## 自检

- 后端：<http://127.0.0.1:8000/health> 应返回 JSON，`ok` 为 `true`  
- 前端：页面顶部应显示后端已连接（需 8000 已启动）

更完整说明见：**`功能说明-上传后全流程.md`**
